# Python Programs

Place small, standalone GUI programs directly in this folder. Use `models/`
for physical simulations and `analysis/` for programs that process experimental
data.

Use descriptive names such as:

```text
tec_display_strip_chart.py
tec_control_gui.py
```
